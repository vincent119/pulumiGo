# Shared global variables available across all stacks.
# Place cross-stack constants here (e.g. AWS account IDs, shared identifiers).
variables:
  # The AWS account ID used for provider allowedAccountIds validation.
  AwsAccountID: "you_aws_account_id_here"
