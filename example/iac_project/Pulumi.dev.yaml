# Stack configuration for the 'dev' environment.
# Defines stack-specific config values used by Pulumi during deployment.
config:
  # AWS default tags applied to all resources in this stack via the AWS provider.
  # Format: <project-namespace>:<key>
  iac_project:iac_project-aws-default-tags:
    Environment: dev
    Project: iac_project
